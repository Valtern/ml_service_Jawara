how to run:

1. Install CMAKE first !

2. run  pip install fastapi uvicorn python-multipart opencv-python numpy scikit-learn dlib easyocr on your terminal

3. run python -m uvicorn main:app --reload --port 8001 (or set whatever port you decided are not used)

